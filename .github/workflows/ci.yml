name: CI

on:
  push:
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer
          coverage: none

      - name: Install Composer deps
        run: composer install --no-interaction --no-progress

      - name: Run phpcbf (Code style auto-fixer)
        run: vendor/bin/phpcbf --standard=phpcs.xml src -s || true

      - name: Run PHPCS (Code style check)
        run: vendor/bin/phpcs --standard=phpcs.xml src

      - name: PHPStan
        run: vendor/bin/phpstan analyse src --memory-limit=1G

      - name: Psalm
        run: vendor/bin/psalm src --output-format=github
